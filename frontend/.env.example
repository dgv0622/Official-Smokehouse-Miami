# Backend API base URL (no trailing slash)
# In development, if unset, defaults to http://localhost:8000
# In production (Cloudflare Pages), set this to your Railway backend URL, e.g. https://your-railway-app.up.railway.app
VITE_API_URL=
